# render.yaml
services:
  - type: web
    name: wiset-deepfake-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    pythonVersion: 3.10 
    autoDeploy: true
